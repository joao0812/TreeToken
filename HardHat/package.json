{
  "name": "tree",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf artifacts cache typechain typechain-types bin",
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.ts --network sepolia",
    "clean:compile": "npm run clean && npm run compile",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "joao0812",
  "license": "MIT",
  "description": "",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/node": "^22.15.17",
    "hardhat": "^2.24.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.3.0",
    "@types/express": "^5.0.1",
    "alchemy-sdk": "^3.5.8",
    "dotenv": "^16.5.0",
    "ethers": "^6.14.0",
    "express": "^5.1.0"
  }
}
